## 1. Types, constants and schemas

- [x] 1.1 Add `STORAGE_KEYS.CAPTURED_WORKOUTS = 'captured_workouts'` in `src/utils/constants.ts`
- [x] 1.2 Create `src/schemas/capturedWorkout.schema.ts`: `CapturedWorkoutPayloadSchema` (tolerant subset of the TrainingPeaks create/update request+response: `athleteId`, `workoutId`, `title`, `workoutDay`, `workoutTypeValueId`, `structure` as string or object, planned metrics, `description`, `coachComments`, `userTags`, `lastModifiedDate`), `CapturedWorkoutRecordSchema` (key `environment:athleteId:workoutId`, ids, `environment`, `capturedAt`, `updatedAt`, `status`, `sentAt?`, `planMyPeakWorkoutId?`, `planMyPeakLibraryName?`, `lastSendError?`, `workout`), `buildCapturedWorkoutKey(environment, athleteId, workoutId)`, and `CapturedWorkoutsStorageSchema` (record map) with a `parseCapturedWorkoutsStorage` helper that keeps valid entries and drops malformed ones
- [x] 1.3 Add runtime message types in `src/types/index.ts`: `WorkoutCapturedMessage` (`kind: 'create' | 'update'`, `athleteId`, `workoutId`, `request: unknown`, `response: unknown`, `timestamp`), `GetCapturedWorkoutsMessage`, `UpdateCapturedWorkoutMessage` (`key`, optional `status`, `planMyPeakWorkoutId`, `planMyPeakLibraryName`, `lastSendError`), `RemoveCapturedWorkoutsMessage` (`statuses`); add them to the `RuntimeMessage` union and `MessageResponse`; add optional `capturedKeys?: Record<string, string>` to `ExportWorkoutsToPlanMyPeakLibraryMessage`
- [x] 1.4 Unit tests in `tests/unit/schemas/capturedWorkout.schema.test.ts`: string structure parsed and polyline dropped, object structure accepted, missing title rejected, malformed map entries skipped, key builder includes environment
- [x] 1.5 Extend the shared export contract: add `itemResults?: Array<{ providerWorkoutId: string; success: boolean; remoteId?: string; libraryName?: string; error?: string }>` to `ExportResult` in `src/export/adapters/base/ExportAdapter.interface.ts`; add `providerWorkoutId` to the `failures` entries of `PlanMyPeakUploadSummary` in `src/background/api/planMyPeak.ts`; add `providerIdNamespace?: string` to `PlanMyPeakExportConfig` in `src/types/planMyPeak.types.ts`

## 2. Main-world detection and capture

- [x] 2.1 Create `src/content/workoutCaptureDetection.ts` with pure `matchTrainingPeaksWorkoutWrite(method, url)` returning `{ kind, athleteId, workoutId, environment } | null` (exact paths `POST …/fitness/v6/athletes/{id}/workouts` and `PUT …/workouts/{workoutId}`, hosts from `TRAININGPEAKS_ENVIRONMENTS`), plus `readJsonBody(body)` for string/`Request` bodies and `safeParseJson(text)`
- [x] 2.2 Unit tests in `tests/unit/content/workoutCaptureDetection.test.ts`: create matched, update matched, sandbox host tagged, `/comments` and `/details` not matched, GET not matched, other hosts not matched, string body parsed, `FormData` body skipped
- [x] 2.3 In `src/content/mainWorldInterceptor.ts` (`fetch` wrapper): when the helper matches, take a synchronous handle on the request body (`Request` input → `input.clone()`, string body → the string) **without awaiting anything**, call the original fetch immediately, and on 2xx clone the response and **return the original response immediately**; a detached promise (`void Promise.all([readBody(requestClone), responseClone.json()]).then(...).catch(...)`) reads the request body and response body concurrently with the page's own consumption and posts `window.postMessage({ type: 'TP_WORKOUT_CREATED' | 'TP_WORKOUT_UPDATED', kind, athleteId, workoutId, request, response, timestamp, source: 'trainingpeaks-extension-main' })`; wrap the capture in try/catch so the original response is always returned
- [x] 2.3a Unit test in `tests/unit/content/mainWorldInterceptor.capture.test.ts` (patching `fetch` in a fresh module scope): (a) with a response whose body resolves after a delay, the caller's promise settles before `postMessage` is called; (b) with a `Request` input whose `text()` resolves after a delay, the original fetch is dispatched synchronously and the caller's promise settles before the body read completes; (c) a thrown error in the clone path does not reject the caller
- [x] 2.4 In the XHR interceptor: hook `XMLHttpRequest.prototype.send` to remember string bodies in a `WeakMap`, and on `load` with 2xx status parse `responseText` and post the same message; keep `open`/`setRequestHeader` behaviour unchanged
- [x] 2.5 Verify no header (including `Authorization`) is included in the posted capture message

## 3. Bridge relay

- [x] 3.1 In `src/content/isolatedWorldBridge.ts`, accept `TP_WORKOUT_CREATED` and `TP_WORKOUT_UPDATED` from `trainingpeaks-extension-main` and forward `{ type: 'WORKOUT_CAPTURED', kind, athleteId, workoutId, request, response, timestamp }` via `chrome.runtime.sendMessage`; add no imports
- [x] 3.2 Unit test in `tests/unit/content/isolatedWorldBridge.capture.test.ts`: forwards both kinds, ignores other sources/types, logs and swallows `sendMessage` rejection

## 4. Background storage service and badge owner

- [x] 4.1 Create `src/services/capturedWorkoutService.ts`: `normalizeCapturedWorkout(kind, payload)` (parse structure string, strip `polyline`, keep the listed fields), `storeCapture(message, environment)` (create → upsert preserving status/capturedAt/sentAt/planMyPeakWorkoutId; update → refresh only if key exists), `listCapturedWorkouts()` (newest first + `pendingCount`), `updateCapturedWorkout(key, patch)` (status transitions, `sentAt`, clear `lastSendError` on sent), `removeCapturedWorkouts(statuses)`, `getPendingCapturedCount()`
- [x] 4.1a Serialize every mutation in `capturedWorkoutService` through one in-memory promise queue (`withCapturedWorkoutsLock(fn)`), so each read-modify-write completes before the next starts; reads may bypass the queue; document that only the background may write the key
- [x] 4.2 Unit tests in `tests/unit/services/capturedWorkoutService.test.ts`: new create stored pending, duplicate create collapses and preserves status, production and sandbox with the same id are distinct records, update refreshes existing and ignores unknown, update of dismissed stays dismissed, mark sent / record error / dismiss / remove, pending count, unbounded insert of 200 records
- [x] 4.2a Concurrency tests: two `storeCapture` calls for different keys started without awaiting both land; `storeCapture` racing `updateCapturedWorkout` on another key loses neither (use a storage mock with an artificial `get` delay to expose the race)
- [x] 4.3 Create `src/services/badgeService.ts` with `refreshBadge()`: export `in_progress` → existing progress text; export `completed`/`failed` with `Date.now() - completedAt < EXPORT_BADGE_LINGER_MS` (the existing clear delay) → existing `✓`/`!`; else pending count → count text (cap `99+`) in amber; else empty. Guard on `chrome.action` availability like `hasActionApi`
- [x] 4.4 Refactor `src/services/exportProgressService.ts` so `updateBadge` and `clearBadgeAfterDelay` delegate to `refreshBadge()` for the idle/clear case instead of calling `setBadgeText('')` directly; export the clear delay as `EXPORT_BADGE_LINGER_MS`; keep the in-progress/completed/failed branches
- [x] 4.5 Unit tests in `tests/unit/services/badgeService.test.ts`: count shown when idle, export wins while in progress, fresh completed state wins, stale completed state (older than the linger window) falls through to the count, stale state after restart shows the count, `99+` cap, empty when nothing pending, no-op without `chrome.action`
- [x] 4.6 Update existing `exportProgressService` tests for the delegated idle branch

## 5. Background message routing

- [x] 5.1 In `src/background/messageHandler.ts`, add `handleWorkoutCaptured(message, sender)`: reject unless `sender.tab` exists and `new URL(sender.url).origin` is a TrainingPeaks app origin; derive `environment` from the sender origin; validate with `CapturedWorkoutPayloadSchema`; call `storeCapture`; call `refreshBadge()`; respond `{ success: true }` or `{ success: false }`
- [x] 5.2 Add handlers and `switch` cases for `GET_CAPTURED_WORKOUTS`, `UPDATE_CAPTURED_WORKOUT` (then `refreshBadge()`), `REMOVE_CAPTURED_WORKOUTS` (then `refreshBadge()`)
- [x] 5.2a In `exportWorkoutsToPlanMyPeakLibrary` (`src/background/api/planMyPeak.ts`), accept an optional `capturedKeys` map; after each workout's POST, when its `provider_workout_id` is in the map, call `updateCapturedWorkout(key, { status: 'sent', planMyPeakWorkoutId, planMyPeakLibraryName, sentAt })` on success or `{ lastSendError }` on failure, before starting the next upload; include `providerWorkoutId` in each `failures` entry; pass `capturedKeys` through from `handleExportWorkoutsToPlanMyPeakLibrary`
- [x] 5.2b Remove the all-failed short-circuit in `exportWorkoutsToPlanMyPeakLibrary`: whenever the upload loop ran, return `success: true` with the full summary (`results` possibly empty, every keyed failure preserved); add `isTotalUploadFailure(summary)` (`results.length === 0 && failures.length > 0`) next to `PlanMyPeakUploadSummary`; update the two consumers, `PlanMyPeakAdapter.export` and `trainingPlanExport`, to call it and produce the same user-facing failure they do today, with `errors` listing every failure message rather than only the first
- [x] 5.2c Unit tests in `tests/unit/background/api/planMyPeak.capturedKeys.test.ts`: outcome written before the next upload starts, success and failure both recorded, message without `capturedKeys` touches no record, failures carry `providerWorkoutId`; all uploads failing returns `success: true` with `results: []` and every keyed failure; update the existing `planMyPeak.test.ts`, `PlanMyPeakAdapter.test.ts` and `trainingPlanExport.test.ts` cases that asserted the old first-error-only shape
- [x] 5.3 In `src/background/index.ts`, call `refreshBadge()` from `chrome.runtime.onStartup` and `onInstalled`
- [x] 5.4 Unit tests in `tests/unit/background/messageHandler.capturedWorkouts.test.ts`: origin gate (popup sender rejected, foreign tab rejected, TP production and sandbox accepted with correct environment), invalid payload rejected, list/update/remove routed, badge refreshed after each write
- [x] 5.5 Confirm `PING.supports` in `buildSiteControlPingResult` and the site-control router are untouched (assert in the existing site-control test that no captured-workout type is served)

## 6. Popup hooks

- [x] 6.1 Create `src/hooks/useCapturedWorkouts.ts`: load via `GET_CAPTURED_WORKOUTS`, subscribe to `chrome.storage.onChanged` for `captured_workouts`, expose `records`, `pendingCount`, `isLoading`, `dismiss(key)`, `clearFinished()`, `refresh()`
- [x] 6.1a In `src/export/adapters/planMyPeak/transformer.ts`, honour `config.providerIdNamespace`: when set, `provider_workout_id` is `${namespace}:${exerciseLibraryItemId}` and `source_file` is `workout_${namespace}_${id}.json`; when unset, behaviour is unchanged. In `PlanMyPeakAdapter.export`, populate `ExportResult.itemResults` from the upload summary (successes with `remoteId` and `libraryName`, failures with `error`), including when `isTotalUploadFailure` is true, and forward `config.capturedKeys` on the export message. Add transformer tests for both namespace cases and adapter tests for `itemResults` on partial and total failure
- [x] 6.2 Create `src/hooks/useSendCapturedWorkouts.ts`: group the given records by environment, build `LibraryItem`s with `normalizeTpPlanWorkoutToPlanMyPeakLibraryItem`, run `planMyPeakAdapter.transform` → `validate` → `export` per environment with `{ createFolder: false, providerIdNamespace: 'cal' | 'cal-sandbox', capturedKeys }`; the hook does **not** write send outcomes (the background does); it records transform-time skips as `lastSendError` via `UPDATE_CAPTURED_WORKOUT`, surfaces `itemResults` and warnings per key for the UI, and exposes `send(keys)`, `sendAllPending()`, `sendingKeys`, `lastSummary`
- [x] 6.3 Unit tests in `tests/unit/hooks/useCapturedWorkouts.test.ts` and `useSendCapturedWorkouts.test.ts`: list + live update on storage change; `createFolder: false`, the right namespace and `capturedKeys` passed per environment; the hook sends no `UPDATE_CAPTURED_WORKOUT` for uploaded items (only for transform skips); identically named workouts attributed by provider id; no TrainingPeaks messages sent

## 7. Popup UI

- [x] 7.1 Extend `TabType` with `'captured'` and add a **New Workouts** tab to `src/popup/components/TabNavigation.tsx` with a `pendingCount` prop that renders a dot and an `aria-label` including the count when > 0
- [x] 7.2 Create `src/popup/components/CapturedWorkoutRow.tsx`: title, workout day, athlete id, sport label from `workoutTypeValueId` (reuse the existing TP→discipline map), duration/TSS, sandbox marker, status chip, `Send to PlanMyPeak` / `Dismiss` buttons, `Sending…` state, error/warning line
- [x] 7.3 Create `src/popup/components/CapturedWorkoutList.tsx`: header with count, `Send all pending` and `Clear sent & dismissed`, rows from `useCapturedWorkouts`, empty state, gating from `useMyPeakAuth`, `useConnectionSettings` and `usePlanMyPeakAccountMatch` (disable actions with hint on missing auth; disable on confirmed mismatch)
- [x] 7.4 Wire the tab into `src/popup/App.tsx`: pass `pendingCount` to `TabNavigation`, render `CapturedWorkoutList` for `activeTab === 'captured'`, reset selections on tab change like the others
- [x] 7.5 Component tests in `tests/unit/components/CapturedWorkoutList.test.tsx` and `CapturedWorkoutRow.test.tsx` (and update `TabNavigation` tests if present): rows rendered newest first, empty state, dot with pending count, send disabled without PlanMyPeak auth and on mismatch, Send/Dismiss/Clear call the hooks, sending state disables actions

## 8. Verification and docs

- [x] 8.1 Run `npm run type-check`, `npm run lint` and `npm run test:unit`; fix anything they report
- [ ] 8.2 Run `npm run build` and load the extension; on TrainingPeaks create a workout on an athlete calendar and confirm the badge shows `1`, the popup lists it, editing it on TrainingPeaks refreshes the row, and Send lands it in the PlanMyPeak default library with provider id `cal:{workoutId}`; re-send confirms an update not a duplicate
- [ ] 8.2a Manual check: start Send all for three workouts, close the popup after the first completes, reopen, and confirm all three show their final state; confirm the badge shows the pending count a few seconds after an export completes rather than staying on `✓`
- [x] 8.3 Update `CLAUDE.md` (project structure + a short "Captured workouts" section noting the badge owner rule and that the capture channel is not page-facing) and `docs/EXPORT_DESTINATION_INTEGRATION_FLOW.md` with the capture pattern as a third initiation path
